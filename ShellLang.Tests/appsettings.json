{
  "ReserverWords": {
    //Reserve(new Token(Tag.KEYWORD, "if"));
    //Reserve(new Token(Tag.KEYWORD, "else"));
    //Reserve(new Token(Tag.KEYWORD, "for"));
    //Reserve(new Token(Tag.KEYWORD, "function"));
    //Reserve(new Token(Tag.KEYWORD, "call"));
    //Reserve(new Token(Tag.KEYWORD, "return"));

    //Reserve(new Token(Tag.TYPE, "integer"));
    //Reserve(new Token(Tag.TYPE, "url"));
    //Reserve(new Token(Tag.TYPE, "file"));
    //Reserve(new Token(Tag.TYPE, "folder"));

    //Reserve(new Token(Tag.BINARYOPERATOR, ">"));
    //Reserve(new Token(Tag.BINARYOPERATOR, "<"));
    //Reserve(new Token(Tag.BINARYOPERATOR, "=="));
    //Reserve(new Token(Tag.BINARYOPERATOR, "="));
    //Reserve(new Token(Tag.BINARYOPERATOR, "+"));
    //Reserve(new Token(Tag.BINARYOPERATOR, "-"));
    //Reserve(new Token(Tag.BINARYOPERATOR, "create"));
    //Reserve(new Token(Tag.BINARYOPERATOR, "copy"));

    //Reserve(new Token(Tag.UNARYOPERATOR, "size"));
    //Reserve(new Token(Tag.UNARYOPERATOR, "delete"));
  }
}
